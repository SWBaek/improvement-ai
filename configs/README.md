# Configs

여러 프로젝트에 공통으로 적용할 설정의 원본과 agent별 adapter를 둡니다.

공통 설정이나 특정 agent adapter가 실제로 필요해질 때만 `configs/<name>/` 디렉터리를 만듭니다. 공통 내용을 agent별 폴더에 복제하지 않으며, 생성 과정이 필요하면 `scripts/`에 생성기와 검증을 함께 추가합니다.

현재 배포할 공통 설정이나 adapter는 없습니다.
